# Knip Export Basics Fixture

This fixture is adapted from `webpro-nl/knip` export and entry-file tests. It
keeps the same conformance intent for unused files, namespace member usage,
entry-file export suppression, default exports, and type-only imports while
shrinking the surface to Codescythe's focused TypeScript dead-code scope.
