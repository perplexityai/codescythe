# Oxc Resolution Fixture

This fixture exercises module resolution that Codescythe delegates to
`oxc_resolver`: tsconfig paths, package `imports`, JavaScript specifiers backed
by TypeScript sources, package `exports`, external packages, and Node builtins.
