# codescythe-linux-arm64

Linux arm64 native N-API package for Codescythe.

This package is loaded automatically by the public `codescythe` package on
`process.platform === "linux"` and `process.arch === "arm64"`. It contains the
`codescythe.linux-arm64.node` binding and is not intended to be used directly.
