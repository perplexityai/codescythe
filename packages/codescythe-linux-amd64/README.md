# codescythe-linux-amd64

Linux amd64 native N-API package for Codescythe.

This package is loaded automatically by the public `codescythe` package on
`process.platform === "linux"` and `process.arch === "x64"`. It contains the
`codescythe.linux-amd64.node` binding and is not intended to be used directly.
